README
==========

CLI
----------

```bash
$ gsu attach {user_name} {group_name}
$ gsu detach {user_name} {group_name}
$ gsu -la
$ gsu -l {user_name}
```

Config
------------

```bash
$ cat ~/.gsu_config
URL: http://localhost:8080
```
